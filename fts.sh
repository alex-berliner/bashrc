function fts {
    sudo apt update -y
    sudo apt upgrade -y
    sudo apt install -y gcc-arm-none-eabi
    sudo apt install -y gcc g++ cmake make bison git git-cola gitk vim
    sudo apt install -y wireguard
}
